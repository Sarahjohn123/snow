class snow{
    constructor(x,y){
        var options={
            'density':0.1,
            'friction':2.0,
            'restitution':1.5,
        }
    }
    
}
this.body=bodies.rectangle(x,y,height,width);
this.image=loadImage(snow4.webp);